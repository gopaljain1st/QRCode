<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cangoproject">
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.Camera.autofocus" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScanCodeActivity"></activity>
        <activity android:name=".Setting" android:parentActivityName=".HomePageActivity"></activity>
        <activity android:name=".AssetList" android:parentActivityName=".HomePageActivity"/>
        <activity android:name=".HomePageActivity">

        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".ItemActivity"/>
        <activity android:name=".Form"/>
        <activity android:name=".ItemDataActivity"
            android:theme="@style/myTheme.NoActionBar"
            />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>